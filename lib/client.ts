export async function api(path:string,body?:unknown){const r=await fetch('/api/'+path,body?{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}:undefined);const data:any=await r.json().catch(()=>({error:'The service is temporarily unavailable.'}));if(!r.ok)throw Object.assign(new Error(data.error||'Could not complete request'),{status:r.status,fieldErrors:data.fields&&typeof data.fields==='object'?data.fields:undefined});return data;}
