# NaTarot Shared UI Architecture LOC Ledger

## Baseline

Measured before implementation on branch `codex/natarot-shared-ui-architecture`, commit `b6eabf2`.

| Scope | Baseline lines |
| --- | ---: |
| Production app/components TS/TSX, excluding API routes | 12,512 |
| `app/globals.css` | 1,965 |
| Tracked test files | 13,680 |
| Tracked Markdown docs | 17,907 |

## After migration

The after counts, additions, removals, proven-dead UI, shared components, test evidence, and remaining issues will be recorded after the final verified build. Counts will remain separated by production source, CSS, tests, and documentation so generated/data growth is not mixed with UI change.
