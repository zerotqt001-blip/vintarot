# VinTarot state

## Product
Moonlight-inspired tarot website. Preserve reference interactions and smooth motion. The visible product brand is now NaTarot; internal component and storage names retain their existing compatibility. User wants real production services but has not provisioned payment/video/email accounts.

## Tarot AI reader engine v4 (2026-09-19, source-only)
The bounded v4 upgrade is implemented in the isolated `codex/tarot-reading-v4` worktree and is not deployed or merged. The NaTarot reader prompt now produces situation-first, evidence-grounded readings with strict v4 list cardinalities, contextual relationship language, practical observable next steps, optional reflection prompts, and context-specific follow-up suggestions. Knowledge Base V5 evidence, upright/reversed meanings, provider/model/temperature/timeout configuration, and transport behavior remain unchanged. Provider responses use the strict v4 schema, while normalized stored-reading validation intentionally retains the wider v2/v3 bounds so historical readings continue to hydrate. Validation: Tarot-focused tests 142/142, tracked tests 227/227, `npx tsc --noEmit`, `npm run build`, `git diff --check`, and a non-persisting DeepSeek provider-backed Vietnamese smoke test pass structurally. No production deployment was performed.

## Tarot AI reader engine v4.1 (2026-09-19, source-only)
The bounded v4.1 refinement remains isolated and undeployed. It keeps the v4 JSON field names, cardinalities, Knowledge Base V5 context, orientations, storage compatibility, routes, providers, models and runtime configuration unchanged while tightening prompt section roles: concise situation-first answers, non-repetitive insights and actions, short position-specific card evidence, optional reflections, calibrated third-party relationship language, and question-shaped follow-ups that never invite another Tarot draw. Generic time-boxed coaching is discouraged unless grounded in the question or spread. Validation: focused v4.1 tests 89/89, all Tarot tests 142/142, tracked tests 227/227, `npx tsc --noEmit`, `npm run build`, and `git diff --check` pass. No v4.1 provider smoke test or production deployment was performed.

## Existing implementation
78 public-domain tarot images, room drawing/shuffling/dragging, guidebook, daily cards, journal, D1 persistence. Local preview on port 5173. Integrations visibly unavailable until configured.

## NaTarot brand system (2026-09-19)
The approved NaTarot rebrand is implemented across the shared shell and route surfaces while preserving the existing Tarot logic, routes, copy, APIs, persistence and responsive interactions. The canonical midnight-navy, antique-gold and ivory tokens live in `app/globals.css`; the reusable wordmark/icon system is `components/brand/logo.tsx` with stable SVG assets under `public/brand/`, and the favicon/layout metadata now use the NaTarot mark. Shared buttons, inputs, panels, sign-in and Room reading surfaces use the same focus, border, elevation and glass-surface language. Home, Guidebook, Create, Practice, Daily Spread, default account/journal/book pages and Room now expose the same brand vocabulary; desktop navigation spacing and default-shell mobile rails were also corrected during browser QA.

Verification for this pass: tracked tests `221/221`, `npx tsc --noEmit`, `npm run build`, `git diff --check`, targeted ESLint for the new brand primitives, and browser checks at desktop `1265×712`, tablet `720px`/`820px`, plus mobile `390×844` for homepage/profile overflow and navigation. Guidebook, Daily Spread, Create, Practice and Room were additionally checked across the desktop/tablet routes; all inspected routes rendered the NaTarot logo, kept header controls separated and stayed within the viewport. The full glob still reports two failures because the preserved user-owned untracked `tests/celestial-surfaces.test.ts` and `tests/homepage-celestial.test.ts` assert an older homepage `cosmic-stars-far`/cursor-motion implementation; those files were not changed. Repository-wide `npm run lint` remains a baseline failure caused by generated worktree/skill artifacts and legacy application rules; no lint exception was added for the new brand primitives. Impeccable review surfaced only existing/reference-aligned font, rounded-border and celestial-grid heuristics. Physical-device motion and provider-backed production behavior remain existing follow-up work.

## Latest feature
/create now offers a custom question, six topics with four localized suggestions each, and a data-driven Step 1 → Step 2 flow. Each topic keeps the NaTarot navy/champagne system with a restrained accent, icon and identity transition; selecting a topic opens its own question list, selecting a suggestion locks `currentQuestion`, and Back returns to Step 1 with the previous topic still selected while preserving typed text. Direct questions bypass suggestions, and skipping a question keeps the selected topic. Create and +Room link here. A one-use sessionStorage draft (1h expiry) transfers question, topic and currentQuestion into room state without exposing question in the URL; existing D1 room and journal persistence retains it. Work prompts were observed in reference; other prompts are VinTarot-authored.
Deck tap starts, second tap pauses the current pose, then a short settle exit opens the fan. The stop action is local UI state, not shared across participants. The shuffle now uses a hands-free 3D cylinder: all 32 full-size cards orbit one central axis continuously with their radial edges aimed inward, symmetric depth shading and a static reduced-motion fallback. Stopping preserves the current rotation before the fan opens. All card-back surfaces now use the supplied custom celestial artwork while card dimensions and face-up artwork remain unchanged.
The interface now supports English and Tiếng Việt through a shared client locale provider. The selector is available in the site header and Room, with a profile selector for signed-in users. The choice is stored in localStorage and mirrored to the D1 profile record; visible navigation, Create questions, Room controls, guidebook, daily spread, journal, practice, profile, booking and invite copy are translated while Tarot card names and stored room data remain unchanged.
Guidebook browsing now follows the reference's category-first flow: Major Arcana, Wands, Cups, Swords and Pentacles appear as animated family cards, each opening its full card grid. Selecting a card navigates to `/guidebook/<card-slug>` with a detail view, card flip and separate upright/reversed meaning copy.
Guidebook and spread-facing card faces now use the Moonlight Rider–Waite CDN artwork and its short labels (Major Arcana numerals and numeric minor-card captions), with the local public-domain artwork retained as an image-load fallback.
Room face-up cards now use the Moonlight artwork's 400×647 aspect ratio (220×356 at the desktop table size) and no longer add a second CSS border, so the title and lower artwork stay visible. Tarot guide copy is localized through `cardMeaning`: Vietnamese Room, Guidebook detail and Practice views use translated keywords and upright/reversed prompts while English keeps the original copy.
Guidebook card details now include an original long-form narrative for all 78 cards. Each entry has a summary plus Energy, Actions, Strengths & Challenges, Relationships, Work, Creativity, Home, Symbolism and Journal Questions sections. The Meme section was removed from every English and Vietnamese reading. `cardNarrative` supplies English and Vietnamese copy, and the detail dialog is scrollable with a Moonlight-like summary callout so the full reading remains usable on mobile.
The long-form Guidebook narrative now follows the selected upright or reversed tab. Reversed cards receive their own summary and ten-section guidance, including a dedicated bilingual The Fool reversal, while the artwork flips with the existing card control.
Room trải bài now has a Moonlight-like interaction pass: cards tilt symmetrically, lift and scale on focus, show contextual prompt pills, and the top-right camera D-pad pans the table with clamped smooth transitions; keyboard arrows mirror the controls. Direct tabletop gestures now let desktop users drag empty space to pan and use the wheel to zoom; touch pointers track a two-finger pinch with the same bounds, while card, fan and drawing targets stay isolated. Fan draws and spread changes use fixed, centered rows aligned to the spread slots, so additional cards no longer land at arbitrary coordinates.
The Room tabletop now uses the supplied celestial artwork as a dark cosmic stage with layered overlays, restrained parallax drift and a 1400px perspective so the spread, fan and card lift read as a balanced 3D scene. The artwork is local to the Site and loads without a network dependency.
The room reading surface now follows the Celestial Luxury Tarot brief: a midnight observatory background, navy/ivory/antique-gold tokens, translucent glass header/guide/toolbar surfaces, a hierarchy for the overview question and reader name, Roman-numeral spread labels with localized prompts, and a small deck-start instruction. A medium-width rule shifts the fixed three-card stage clear of the guide panel while preserving the existing card dimensions, assets and handlers; reduced-motion overrides keep the layered scene calm.
The 2026-09-17 mobile Room pass keeps the tabletop centered with a single CSS transform model, constrains the fixed surface to the device viewport, lifts the card fan above the bottom rail, and wraps the deck metadata so the toolbar remains balanced inside safe areas. Fan selection now accepts touch pointers reliably, shows a ghost while dragging, and places the selected card directly into its deterministic spread slot; keyboard selection, desktop drag/pan, and reduced-motion fallbacks remain intact.
The 2026-09-17 Room copy simplification keeps the three spread position names (Persona, Obstacle and Solution) while removing their secondary hint lines, the tabletop reader avatar/name, and the question-owner name. The deck prompt is now a subdued `NaTarot.com` watermark so the fan remains the visual focus.
Guidebook overview now presents the same five families in a responsive constellation map inspired by the supplied infographic: a central 5-group/78-card hub, connected Major Arcana, Wands, Cups, Swords and Pentacles nodes, localized descriptions and existing clickable card-grid dialogs. Card detail routes and their long-form upright/reversed readings remain unchanged.
The latest Guidebook pass replaces the former white dialog flow with three immersive NaTarot screens. `/guidebook` is a dark cosmic five-world map with animated circular portals; selecting a family pushes a `?group=` URL and opens a dedicated library with a family sidebar, search, upright/reversed filter, classic/A–Z sorting and a responsive five/three/two-column card grid. Selecting a card opens a full-page cosmic detail view with uncropped Rider–Waite artwork, thumbnail navigation, previous/next controls, upright/reversed tabs, the complete bilingual narrative and a room CTA. Detail navigation updates history without a white flash and supports keyboard arrows and horizontal touch swipes while respecting family boundaries.
Home navigation labels and the Create Ritual control now use continuous SVG text paths, matching Moonlight's curved label treatment in both English and Tiếng Việt. Accessible link names remain intact while the visible phrases no longer stack or rotate as a single block.
The homepage now has a NaTarot Celestial portal treatment: a layered cosmic scene with the local celestial artwork, star/nebula/planet/architecture/floor/foreground depth layers, pointer parallax on fine pointers, scroll-aware motion variables, and reduced-motion fallbacks. Header, floating sidebar, hero create-room CTA, daily spread panel, quote, feature cards, footer and help dialog use midnight navy, antique gold, ivory and translucent glass tokens. EN/VI header taglines and the daily quote are localized, while routing, card assets and existing actions remain unchanged.
The Practice route now uses the same Celestial Sanctuary visual system: a dark observatory scene with restrained stars, nebula and architecture layers, a two-column card/reflection composition, a framed Moonlight card with hover lift and replacement transition, a dark reflection surface with character count, animated interpretation panel, and a premium “Read together” room card. Existing random-card, reveal, journal-save and `/room` actions remain unchanged, with responsive stacking and reduced-motion fallbacks.
The `/create` ritual now follows the supplied NaTarot reference composition: a full-width midnight observatory shell, a centered “ĐẶT MỘT CÂU HỎI” topic step with six gold/rose topic orbs, and a keyed transition into a focused topic-question step with back navigation and four suggestion rows. The topic identity, active state, typed question behavior and skip/back handlers remain intact; the new step rail, responsive cluster, full-width flex sizing and reduced-motion fallback are CSS-only presentation additions.
The primary card navigation now reads “Cards” / “Lá bài” and points directly to `/guidebook` in both the header and sidebar. The former `/decks` page is retained only as a compatibility alias to the Guidebook, its deck-specific screen and switcher are removed, and the homepage card-library CTA now opens the Guidebook; the Room’s existing deck control remains available for the reading flow.

The 2026-09-19 `/daily-spread` pass now uses a dedicated full-width Celestial Observatory shell, shared NaTarot midnight navy/champagne/ivory tokens, responsive top/sidebar/bottom navigation, a centered cosmic two-card stage, and a reflection panel with gold primary/secondary actions. Existing flip, detail and journal/sign-in behavior remains unchanged; the `/cards/vintarot-card-back.png` asset is explicitly preserved at its original 2:3 ratio. Focused Daily regression, `npx tsc --noEmit`, production build and `git diff --check` pass; browser QA covered desktop rendering, the 390px bottom navigation/no-horizontal-overflow layout and hidden unrevealed-face accessibility. The public site was initially source-only, then deployed on 2026-09-19 from runtime commit `f6cd053`; public Daily HTML, card-back asset, catalog API and two-card reveal/reflection flow returned successfully, with the previous VPS release retained for rollback.

The latest Room mobile pass additionally passed the tracked test suite plus `tests/room-mobile.test.ts` (54/54), `npx tsc --noEmit`, `npm run build`, and `git diff --check`. Local browser checks at the requested phone-sized viewport verified `scrollWidth === innerWidth`, centered tabletop bounds, a balanced deck summary with safe-area padding, and both tap and drag fan selection inserting cards without a release animation; the desktop Room bounds remained centered after resetting the viewport. The full glob also includes two pre-existing untracked homepage assertions that remain red against their older `cosmic-stars-far` expectation; they were left untouched.

The latest Room simplification was verified at 390px and desktop: the avatar and reader names are absent, Persona/Obstacle/Solution remain without hint copy, and the `NaTarot.com` watermark is subdued.

## NaTarot Reading Experience V1 (2026-09-19)
The approved post-reading redesign is implemented as a presentation-only ReadingPanel update. The existing v4.1 fields remain the source of truth: the first `direct_answer` paragraph becomes the single highlighted takeaway, remaining answer prose follows naturally, the first two `personal_insights` and `next_steps` are prominent with additional items behind lightweight `Xem thêm` disclosures, and `deeper_reading` renders only when present. Card evidence is collapsed by default and preserves every existing card, position, orientation, interpretation and identity attribute when expanded; reflection prompts are secondary and render only when non-empty. Up to three existing `follow_up_suggestions` remain clickable prefill actions using the current follow-up request flow, with the custom question input unchanged.

The completed-reading layout gives the reading approximately 60–64% of the desktop width while retaining the card/tabletop area as the visual anchor. Mobile uses one vertical flow with comfortable touch rows, safe bottom padding and no nested dashboard-card treatment. The existing journal save action remains unchanged. Secure share links, polished image export and paid continuation were audited but not invented because their required public-reading, export and billing infrastructure is not present. No Reader Engine, prompt, schema, provider, Knowledge Base, database, card logic or authentication changes are part of this slice.

Validation for this slice: focused UI/Room tests `35/35`, all Tarot tests `147/147`, full tracked tests `240/240` across 55 files, `npx tsc --noEmit`, production `npm run build`, and `git diff --check` pass. The rebuilt local Wrangler preview served Room, current CSS/Room assets and the Tarot catalog successfully; desktop Room rendering was inspected without a new provider-backed reading request. Reopening an existing saved reading through the current browser path still requires its existing POST interpretation flow because the Room GET path does not hydrate saved reading payloads; this remains an existing architecture gap and was not expanded here.

## NaTarot Reading Experience V1.1 (2026-09-19)
This bounded visual-polish pass keeps the approved V1 ReadingPanel architecture and all v4.1/follow-up behavior unchanged. The desktop question is reduced to a subordinate 1.4rem–2rem responsive scale with a 38ch measure; the takeaway is now one editorial gold rule with open space instead of a dashboard rectangle; direct/deeper/follow-up prose is capped at a 72ch measure; and the single reading scroll surface now sizes to content before shrinking, with compact 28px–48px bottom breathing room and a separated, non-sticky action footer. Mobile keeps a 24ch question measure, safe-area-aware compact 24px–36px footer breathing room and full-width action rows without introducing nested scrolling. Four-card spreads retain their row positions while increasing only the shared stage scale from 0.84 to 0.9; orientation, order, positions, cards, assets, background and question content remain unchanged.

Validation for the final V1.1 polish: focused Reading UI/Room tests `24/24`, all tracked Tarot tests `152/152`, full tracked tests `246/246`, `npx tsc --noEmit`, production `npm run build`, and `git diff --check` pass. The rebuilt local Wrangler preview on port 5187 returned `200` for the homepage, Room, catalog, local celestial artwork, logo and all 16 referenced hashed CSS/JS assets; the browser reported no warning/error entries, and the 1280×720 plus 390px checks kept document width equal to the measured viewport width. The pre-rebuild in-memory reading snapshot was inspected for the complete header → answer → insights → actions → reflection → evidence → follow-up → footer structure; the post-rebuild CTA was not used because it would make a provider call. No provider request was initiated by this polish pass. No Reader Engine, prompt, schema, provider configuration, Knowledge Base, persistence, API, follow-up logic or untracked user file changed.

The 2026-09-17 mobile navigation pass moves the four primary links (Home, Cards, Practice and Book session) from the vertical sidebar into a fixed, full-width bottom bar on the homepage and non-Room shells. The bar uses safe-area spacing, readable mobile labels, preserved desktop rail styling and extra content clearance; the redundant mobile footer, duplicate header nav and secondary personal links are hidden. Room keeps its dedicated tabletop controls and bottom deck toolbar. The tracked suite (49/49), production build and diff check pass; local browser checks confirm the desktop rail, `/create` shell and Room controls remain intact. Lint still reports pre-existing generated-skill/runtime and legacy anchor-rule errors, and the two untracked homepage assertion files remain outside this change.

The 2026-09-17 Room parity pass brings the visible Moonlight Room tools into the NaTarot observatory without replacing its cosmic identity: the text tool now places editable annotations at the tapped table position and persists them through the bounded shared-room API schema; the spread control is a side panel with the reference topic labels Relationships, Planning, Moon Phase, Creativity, Business and Fool’s Journey; and “Move cards back” is separate from “Start again”. Fan tap, keyboard draw and drag share one direct draw path, so the card is inserted immediately into its deterministic spread slot without a release animation. A medium tablet rule scales and shifts the fixed table clear of the guide panel, while the spread panel moves into a bottom sheet on mobile. Local browser checks verified the topic panel, text placement, text persistence after reload, tap draw, drag selection, and the 390×844 responsive layout. Tracked tests pass 54/54, `npx tsc --noEmit`, `npm run build`, and `git diff --check` pass; full lint remains red across the repository’s existing generated-runtime/legacy-rule baseline. Video/audio service setup remains intentionally unavailable until real accounts are provisioned.

The 2026-09-17 Room header refinement removes the `TAROT READING` kicker and its separator, promoting the user’s question into that upper header position on desktop and mobile. The existing cosmic background, controls and question typography remain unchanged.

The 2026-09-17 Create shell refinement removes the rectangular border and shadow chrome from the desktop Create shell and its two left navigation panels so the cosmic background reads more openly. The translucent surfaces, circular topic controls and capsule question input remain unchanged; the mobile bottom navigation keeps its existing border and shadow treatment. The focused regression test, tracked suite (51/51), production build and local Create/Room visual checks pass. The public Site remains public and is live at version 4 from the exact published source commit.

The latest Room draw refinement removes the post-selection `deal-in` and `fan-release` animations together with their timeout and origin-measurement path. Selecting by tap, keyboard or swipe now commits the card directly into its deterministic spread slot, removing the reported hitch while preserving the fan drag ghost and card focus/reveal interactions. The tracked suite (54/54), `npx tsc --noEmit`, production build and `git diff --check` pass; the public Site is live at version 5 from source commit `9834e8b`.

The 2026-09-18 Room mobile-first pass keeps the existing observatory, shuffle, draw, persistence and desktop toolbar behavior while adding a compact mobile header with NaTarot home navigation, locale, theme and menu actions. At widths up to 768px, the permanent media, zoom, pan and editor rails are hidden from the reading surface and remain available through Radix dialog bottom sheets; the sheet includes the existing drawing, text, theme, reversal, spread, card finder, return, guidebook, save, reset, media, fullscreen, zoom and pan actions. The welcome guide becomes a restrained mobile hint, the question/card/CTA composition uses the device viewport and safe-area insets, and the bottom deck summary remains visible. English and Vietnamese labels were added for the new menu surfaces. The focused mobile-first tests (3/3), tracked suite (54/54), `npx tsc --noEmit`, production build and `git diff --check` pass. Local browser render was checked on the retained desktop preview; mobile breakpoint coverage is enforced by source/CSS tests and still needs physical-device confirmation.

The mobile-first source was committed and pushed as `b9d5a25`. After the user explicitly requested a new public link, a separate Site was created and deployed from source commit `cd4ea85` as version 1 at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`; the previous public URL remains unchanged.

The 2026-09-18 follow-up fixes the Room mobile dialog alignment. Radix's shared dialog centering uses Tailwind's independent `translate` variables, and the production CSS optimizer can fold a direct `translate:0 0` declaration into `transform`; the mobile bottom sheet now explicitly resets `--tw-translate-x` and `--tw-translate-y` to zero so it stays flush with the device viewport after compilation. At 390×844 the sheet measures `x=0`, `width=390` with `scrollWidth=390`, matching the intended full-width mobile surface. The focused regression (4/4), tracked suite (80/80), production build and diff check pass. The dynamic-draw and interpretation work that followed is recorded in the release note below.

The 2026-09-18 dynamic Tarot system is now wired through the Room flow. The six Moonlight topics are backed by a canonical catalog of seven spread templates and ordered positions; D1 migrations create normalized catalog, card, meaning, reading-session, reading-card and interpretation tables, and an idempotent deployment migration seeds the 78-card Rider–Waite–Smith deck plus 312 bilingual orientation meanings. Room loads the catalog with a local fallback, requests an exact server draw plan before the shuffle, consumes each card once into its server-provided position, keeps guest sessions behind an HttpOnly identity, and shows a bilingual Reflection panel with a local interpretation fallback or optional provider metadata. Journal save carries the dynamic session metadata and still opens sign-in for unauthenticated users. The implementation is committed and pushed through `c238cc8`.

The 2026-09-18 Knowledge Base V5 and AI Tarot engine pass replaces the old optional/local interpretation path with a provider-neutral server reading flow. The complete user-supplied V5 package is checked in under `natarot-knowledge/v5`; runtime retrieval uses only the drawn cards, their stored D1 positions/orientations, relevant handbook evidence, bounded combination guidance and at most two same-language drawn-card examples. V5 evaluation material is retained for testing/reference and is never assembled into a production prompt. Existing D1 card IDs and spread/session records remain the identity source; V5 card names are an explicit interpretation-layer lookup and do not migrate or replace database identity.

`POST /api/tarot/reading` is the canonical endpoint. It validates ownership and the historical spread definition, requires both requested-locale orientation rows, calls the explicitly selected OpenAI/Gemini/DeepSeek provider, validates the strict `{overview,cards,connections,guidance,closing}` contract, enriches card/position metadata from trusted storage, and persists compatibility reading columns plus provider/model/prompt metadata. `POST /api/tarot/interpret` is a thin compatibility alias. The Room reads the canonical payload, keeps the full randomized 78-card customer fan and dynamic spread layout, and shows the structured Tarot reading panel without a local AI fallback claim.

The server environment contract is `TAROT_AI_PROVIDER=openai|gemini|deepseek` plus only the selected provider's key/model pair: `OPENAI_API_KEY` and `OPENAI_TAROT_MODEL`, `GEMINI_API_KEY` and `GEMINI_TAROT_MODEL`, or `DEEPSEEK_API_KEY` and `DEEPSEEK_TAROT_MODEL`. All provider keys are server-side secrets and must not enter client code, browser storage, committed files, logs or public responses. Missing selected-provider configuration produces a safe unavailable/retry response and never switches providers. The README contains placeholders only. No new reading migration was required: the normalized payload continues to use the existing `readings` compatibility columns. `lib/tarot-narrative.ts` remains available to Guidebook and Tarot seed consumers only and is not a production final-reading fallback.

Task 6 verification on 2026-09-18: `npx tsx --test tests/tarot-documentation.test.ts` passed 3/3 after its expected failing TDD run; `npx tsc --noEmit`, `npm run build`, and `npx eslint tests/tarot-documentation.test.ts` exited 0. The required nine-file command `npx tsx --test tests/tarot-ai.test.ts tests/tarot-interpretation.test.ts tests/tarot-reading-context.test.ts tests/tarot-reading-service.test.ts tests/tarot-api-contract.test.ts tests/tarot-draw.test.ts tests/tarot-catalog.test.ts tests/tarot-seed.test.ts tests/tarot-room.test.ts` first passed 68/68, then briefly exposed three failures while concurrent Room/i18n changes were incomplete, and finally passed 75/75 after those changes landed separately as `cef4180`. Repository-wide `npm run lint` remains red on the unchanged generated-skill/runtime and legacy application baseline: it exited 1 with 11,460 problems (1,851 errors and 9,609 warnings), including `.codex/skills`, `.sites-runtime`, legacy `@next/next/no-html-link-for-pages` errors in `app/vintarot.tsx`, and existing `no-explicit-any` errors in `app/webmcp.tsx` and `lib/client.ts`. The complete test glob was not rerun for Task 6; the two user-owned untracked homepage assertions remain outside this slice.

The updated public Site was published earlier as version 11 from source commit `73280ff2d8a62f272ede1356f89ec83290abfe03` at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`. Task 6 does not publish a new deployment. No provider secret has been configured yet, so live AI generation remains safely unavailable until one selected server-side provider key/model pair is configured in the deployment environment.

The dynamic release was published to the existing public mobile Site as version 4 at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`. Production D1 inspection confirms the Tarot tables and seeded rows are present. Verification after the final source change: `npx tsc --noEmit`, production `npm run build`, targeted ESLint for `lib/tarot-interpretation.ts`, the focused Tarot/Room suite (20/20), archive contents, and `git diff --check` pass. The full test glob remains 82/84 because the two pre-existing untracked homepage tests still expect the older `cosmic-stars-far` class contract; repository-wide lint remains red on the existing `.codex`/`.sites-runtime` files and legacy anchor rules outside this feature.

The 2026-09-18 Room spread recovery fixes a race that could replace the prepared server draw order with an empty array after shuffle, leaving the spread slots visible but the fan blank. The prepared plan is now returned through the async flow, each dynamic card is revealed into its deterministic position, and the completed three-card reading exposes “Xem lời giải Tarot” plus “Rút lại và xào bài”. The new interpretation surface follows the supplied reference with a right-hand overview panel, four tabs (overview, card details, message and actions), position thumbnails, synthesis, advice and reflection prompts; it remains responsive on the mobile Room. Verification: focused Tarot/Room suite (5/5), `npx tsc --noEmit`, production `npm run build`, archive validation, `git diff --check`, and live browser checks for shuffle → three-card draw → CTA → loaded interpretation → tab switch → close. The full test glob remains 83/85 because the same two untracked homepage assertions still expect `cosmic-stars-far`. This fix is committed and pushed as `5a27e44` and published as version 5 at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`.

The Room follow-up hardens asynchronous reading flow: draw-plan and interpretation responses now carry epoch/session/request guards, and reset, redraw, spread changes and remote state hydration invalidate older work so stale responses cannot resurrect or overwrite a newer reading. The mobile guide label regression was restored to the existing guidebook copy. Verification: focused Tarot/Room suite (6/6), `npx tsc --noEmit`, production `npm run build`, archive validation, `git diff --check`, and live browser checks for the production shuffle → three-card spread → interpretation CTA → loaded four-tab panel flow. This follow-up is committed and pushed as `fddb547` and published as version 6 at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`.

The latest Room draw correction keeps the customer-facing fan as the complete shuffled 78-card deck. Every shuffle uses the unique full deck order, so each 78-card pass contains every card exactly once and each new pass gets a fresh random order. Shuffle no longer asks the API to preselect three cards; tap, keyboard, CardPicker and swipe selection all remove only the card the customer actually chose, place it into the next deterministic spread slot, and preserve the existing hover lift/drag ghost motion. After the final required card is selected, Room sends the exact card numbers and orientations to the draw API; the server maps those selections to spread positions and the later interpretation reads only that persisted session. Duplicate/unknown selections are rejected, and a failed session creation exposes a retry action without losing the local spread. Verification: focused Tarot/Room/API suite (20/20), `npx tsc --noEmit`, production `npm run build`, and `git diff --check` pass. Repository-wide lint remains red on the pre-existing generated/runtime and legacy anchor-rule baseline.

The 2026-09-18 Moonlight spread-catalog parity pass adds all nine reference topic groups and 57 inspected child spreads to the Room picker, including the Blank reset surface. Each template now carries an explicit semantic layout family and ordered positions: rows, compact 4/5-card lines, triangle, top-plus-bottom, Yes/No branches, Conflict/Bridge/Path and Celtic Cross. The picker previews use those layouts, and the full 78-card fan, random unique shuffle order, customer-selected card mapping, hover lift and interpretation flow remain unchanged. Local visual checks covered representative branching, cross and five-card arrangements; the tracked test suite passes 91/91, `npx tsc --noEmit`, production `npm run build`, and `git diff --check` pass. The Sites-safe catalog migration preserves historical spread-position IDs and was published as version 10 to the public mobile Site from source commit `46485ba10627b6323bd8ea7e16c7836ffec162b2`.

The final Tarot engine review follow-up hardens the selectable provider transports: Gemini uses its full JSON schema through `responseJsonSchema`, DeepSeek receives an explicit exact-key JSON contract, and selected provider keys/models are trimmed before transport. Money/debt questions now receive specific non-advisory financial reflection guidance. The catalog migration preserves canonical relationship IDs on fresh databases while retaining historical `reading_cards` foreign-key IDs when legacy aliases are already stored; it remains idempotent and does not delete catalog rows. Final verification on 2026-09-18: `npx tsx --test tests/tarot*.test.ts` passed 101/101, `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. Repository-wide `npm run lint` remains red at 11,459 problems (1,850 errors and 9,609 warnings) in the existing generated/runtime and legacy application baseline; no provider secret or live provider credential was used.

AI reading engine Task 3 (2026-09-18) adds trusted D1 context and compatibility persistence mapping. Historical spread lookup uses the stored template ID even when its category/template is inactive; both requested-locale meaning orientations are required. The builder validates session/card/position identity and order, maps complete localized D1 evidence, and retains the user-provided Knowledge V5 layer only as supplemental guidance. V5 examples are restricted to the requested locale and drawn cards. Four imported V5 JSON assets, existing types/fixture additions, and data serialization are included; the separate user-owned system-prompt changes remain unstaged because prompt safety is outside Task 3. The focused context/repository/Task 1–2/draw/seed/Guidebook suite passes 57/57, scoped TypeScript and ESLint pass, and diff checks pass. Full-project typecheck at verification time reports 19 errors in Room's old reading payload fields; that UI integration remains outside this slice. No schema migration, provider API, draw, route, Room or Guidebook/seed narrative implementation changes were made by Task 3. Details and exact command output are in `.superpowers/sdd/2026-09-18-ai-tarot-reading-engine/task-3-report.md`.

Email delivery setup on 2026-09-18: the `natarot.com` domain was added to Resend and verified successfully after adding the provider's DKIM TXT, SPF CNAME (`rsend` and `send`) and optional DMARC TXT records in BKNS DNS. Resend now reports the domain as ready to send; no mailbox was created because outbound authentication/reset emails only require a verified sending domain. A least-privilege `natarot-email-sending-live` API key with Sending access is stored only in the VPS `/etc/natarot.env` secret file; the service was restarted and verified active, while the secret remains out of the repository and project state. Registration, login and password-reset email delivery still need the application code and sending endpoint wired to this environment variable.

Personal-centric AI Tarot reading redesign (2026-09-18): the approved V3 provider contract now normalizes into a typed `TarotReadingPayload` with direct answer, personal insights, reflection prompts, next steps, card evidence and optional follow-up context. The `reading_payload` compatibility column and ordered `0004_reading_payload.sql` migration preserve the normalized payload while legacy columns remain populated for older consumers; the custom Node migration runner now discovers ordered `NNNN_name.sql` files. `POST /api/tarot/follow-up` is owner/session/card checked, provider-neutral, bounded, bilingual and deliberately does not persist a transcript. The Room reading surface now presents the question and direct answer first, then personal insights, reflection prompts, next steps, evidence disclosure and the follow-up panel; the panel keeps answers across prompt reselection and resets only when the reading epoch changes.

Verification for this slice: `npx tsx --test tests/tarot*.test.ts` passed 137/137 after the final parser/hydration review fixes; `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. `npm run lint` remains a repository-wide baseline failure with 11,670 problems (1,937 errors and 9,733 warnings); the targeted Room/reading lint reports 37 existing Room errors/warnings (30 errors, 7 warnings), primarily legacy `any` and Next anchor rules, with no new component/test lint signal. Browser QA used the production-local Wrangler preview: the default desktop Room shell rendered correctly, shuffle advanced to the draw state, and 390px/tablet responsive accessibility states exposed the compact header, spread labels and full card fan. Provider-backed reading QA is blocked in this local runtime because both `/api/rooms` and `/api/tarot/catalog` reproduce `TypeError: Illegal constructor` in dev and production-local Wrangler modes; therefore no manual provider-backed reading or reading-panel screenshot is claimed here. The deployment path with Node 22/D1 and real provider configuration still needs a separate post-migration smoke test, and physical-device motion remains unverified.
The final independent re-review of the Tarot feature passed after commit `e52123f`: no Critical, Important, or Minor findings remained. The user-owned untracked homepage/celestial files were preserved and kept out of the feature commits.

Reading-surface correction (2026-09-18): history review confirmed that the pre-redesign Room interpretation panel used the existing dark navy/cosmic gradient, not an ivory/cream answer block. The direct-answer section now uses that dark palette with explicit ivory body text and antique-gold heading text; the missing Tailwind color tokens are declared so those utility classes compile into the production CSS. Focused reading/Room/theme/backdrop tests pass 24/24, `npx tsc --noEmit`, `npm run build`, and `git diff --check` pass. Local browser provider-backed reading remains blocked by the known `Illegal constructor` runtime failure in `/api/rooms`; no manual reading-panel screenshot is claimed.

Cloudflare local-runtime correction (2026-09-19): `/api/rooms`, `/api/tarot/catalog` and other D1-backed route handlers no longer misclassify Cloudflare's `process` compatibility shim as standalone Node. The runtime bridge now resolves `cloudflare:workers` first and selects the D1 binding explicitly, falling back to `node:sqlite` only when the Cloudflare module is unavailable. The Cloudflare/Vinext toolchain was aligned to `@cloudflare/vite-plugin` 1.56.0, `@cloudflare/workers-types` 5.20260918.1, `@vitejs/plugin-rsc` 0.5.35, `vinext` 1.0.0-beta.10 and Wrangler 4.135.0. Regression coverage, 139 Tarot tests, `npx tsc --noEmit`, production build and live local API smoke tests pass; `GET /api/rooms` and `/api/tarot/catalog?locale=vi` now return successfully. The earlier local reading-panel screenshot blocker is resolved at the runtime layer; provider-backed AI output still depends on the configured provider contract.

Post-redesign VPS release (2026-09-18): source commit `5ceac8f812edac6005126aa914e6a1037a5d3a01` was packaged without `.env*`, databases, logs or provider credentials and installed at `/opt/natarot`; migration `0004_reading_payload.sql` was applied idempotently, while the previous tree remains at `/opt/natarot.previous-20260918-134735` for rollback. The first source-only restart correctly exposed that Vinext production start requires the verified `dist/` artifact, so the local production build was separately secret-scanned and installed before the final restart. `natarot.service` is now active and the public build exposes the updated Room asset. The final HTTPS smoke test returned homepage/Room/catalog `200`, draw `201`, session/room/journal `200`, and canonical reading `503` with the safe no-provider message. The public Room was reloaded at `https://natarot.com/room?ritual=1` and rendered without a browser console error. No provider key/model is configured on the VPS, so this release does not claim live AI output.

Latest NaTarot/VPS deployment (2026-09-19): the public release was rebuilt from source HEAD `5fb0e4e` plus the preserved working-tree `app/globals.css` and `app/vintarot.tsx` changes, packaged with no `.env*`, database, log, `node_modules` or credential content, and installed atomically at `/opt/natarot`. `natarot.service` is active with build ID `efae424d-7ff3-4267-a9d0-4a4d298d7875`; `/opt/natarot.previous-20260918-193541` is retained for rollback. Final HTTPS smoke checks returned `200` for `/room?ritual=1`, `/api/tarot/catalog?locale=vi` and `/api/rooms`. The live CSS bundle is `layout.DkaqPvka.css`, contains the dark `reading-section--direct-answer` rule and shared brand primitives, while the old cream token is absent. Brand token/primitive contracts, `npx tsc --noEmit`, production build and `git diff --check` pass; the two preserved untracked homepage tests still fail on their older `cosmic-stars-far` expectation and were not changed.

Latest header-safe NaTarot deployment (2026-09-19): commit `efda23805116afcc53c2484fc177ccc69dcc7162` extends the medium-width header compaction through 1100px after public browser QA found the tagline crowding navigation near 1030px. The secret-scanned 371-file archive was built, installed atomically at `/opt/natarot`, migrated successfully, and `natarot.service` is active; `/opt/natarot.previous-efda23805116afcc53c2484fc177ccc69dcc7162` is retained for rollback. Public HTTPS smoke checks returned `200` for `/`, `/guidebook`, `/room?ritual=1`, the brand SVG and `/api/tarot/catalog?locale=vi`; the public homepage browser check shows the NaTarot logo, midnight-gold visual system and separated header controls. Tracked tests remain `221/221`, with typecheck, production build and diff check passing. No provider secrets or runtime database were copied.

Room guidebook behavior correction (2026-09-19): the Room guide panel now starts closed, selecting or flipping a drawn card no longer opens it automatically, and the BookOpen toolbar control remains the in-room toggle. The focused Room/UI tests pass 32/32, `npx tsc --noEmit`, `npm run build`, `git diff --check`, and local browser smoke checks pass; this source fix is committed as `04b2df3` and pushed to `codex/tooling-and-version-history`. No public deployment was performed for this small interaction change.

Room guidebook production deployment (2026-09-19): source HEAD `2b2ab2a66e279594d1905993cf7d08d09da8a47` was rebuilt, secret-scanned, and deployed atomically to the existing VPS release at `/opt/natarot`; the previous tree is retained at `/opt/natarot.previous-2b2ab2a66e279594d1905993cf7d08d09da8a47` for rollback. The service remains active and the public HTTPS smoke checks returned `200` for `/`, `/room?ritual=1`, and `/api/tarot/catalog?locale=vi`. A live browser check confirmed the Room guide is closed initially, selecting a fan card keeps it closed, and the BookOpen/Sách hướng dẫn control opens it. The VPS release reuses the existing dependency tree through a symlink to avoid duplicating `node_modules`; no database or `/etc/natarot.env` changes were made.

Room mobile/language/zoom redeployment (2026-09-19): the first `07d9ed5` release was later overwritten on the VPS by a concurrent older release, so the verified source was rebuilt and redeployed from commit `daccb2efadeadb6c80aa03424e79a1aea1e6f9fd` using a unique release path. The active `/opt/natarot` source now has `guide=false` and `zoom=.7` before and after restart; `natarot.service` is active; and `/opt/natarot.previous-daccb2efadea-final` is retained for rollback. The first-load locale falls back to Vietnamese without overwriting an existing explicit localStorage/profile preference; the ready-phase Welcome guide is hidden on mobile; card selection leaves the guide closed; and BookOpen opens the card information manually. Focused Room/language/motion tests pass `29/29`, the tracked Tarot/Room suite passes `170/170`, `npx tsc --noEmit`, production `npm run build`, public HTTPS smoke checks and browser QA pass. No database or `/etc/natarot.env` changes were made.

## Room composition and interaction cleanup (2026-09-19)
The approved Room UI cleanup is implemented locally. Mobile no longer mounts the ready-phase Welcome guide; desktop keeps the existing guidebook surface. The legacy `room-bottom` deck/reflection bar and shuffle CTA are removed, the tabletop now reaches the released viewport height, and the existing `ShuffleDeck` click handler remains the sole shuffle entry point. Drawn cards are non-button pointer surfaces, so mouse/touch/pointer selection, reveal and drag remain available without direct card activation opening meaning; BookOpen remains the guide entry point. Obsolete Room translation strings for the removed controls were deleted, and the visible status surface no longer captures pointer events except its actionable buttons.

Validation: focused Room/UI tests pass 35/35, the tracked test suite including the new cleanup regressions passes 231/231, `npx tsc --noEmit`, `npm run build`, and `git diff --check` pass. Local browser QA covered exact viewports 390×844, 393×852, 430×932, 1440×900 and 1920×1080: no horizontal overflow, no `.room-bottom`, no `.shuffle-control`, no ready Welcome guide in mobile DOM, tabletop bottom is 0px, and the bottom status wrapper has `pointer-events:none`. Desktop BookOpen still mounts the ready guide; one deck click changes to the active shuffle state; direct card click keeps guide/dialog closed. The live preview reported no console warnings/errors. Safe-area/dvh CSS support was checked in the Chromium local preview; a physical iOS Safari device was not used. Repository-wide lint remains red on pre-existing generated/runtime and legacy application rules; targeted lint remains red on pre-existing Room `any`/anchor rules. Existing user-owned untracked files were preserved and kept out of the change.

## NaTarot Saved Reading V1 (2026-09-19)
Saved Reading V1 adds authenticated ownership for an already-persisted Tarot reading without adding a migration: the existing `records` table stores an owner-scoped `kind=tarot-reading` marker keyed by `saved-reading:<reading_id>`. Save validates the trusted authenticated owner, exact session, and exact persisted reading before an idempotent marker upsert. The interpretation response now includes the persisted `reading_id`; the Reader Engine, prompt, provider configuration, Knowledge Base V5, orientations and ReadingPanel visual design remain frozen.

Journal history lists saved Tarot readings separately from manual journal entries. Opening a saved item performs only `GET /api/tarot/saved-readings?id=...`, parses the stored row with `parseStoredReading`, rehydrates trusted card/position/orientation metadata, and renders the existing ReadingPanel. It does not call `createTarotAIProvider`, `generateTarotReading`, DeepSeek, or the interpretation POST route. Nullable `reading_payload` rows continue through the v2/v3 compatibility path.

Guest users retain the private room/session and reading flow, but Save opens sign-in and does not create a persistent guest journal. The existing architecture does not provide a trusted guest-to-account association mechanism, so no post-login claim shortcut or regeneration was invented; this gap is intentionally documented and the frozen reader is not called again by the saved-reading reopen path. Share links, image export, billing and credits remain out of scope.

Validation: the focused Saved Reading/reader/identity matrix passed 46 top-level subtests / 52 tests; all Tarot tests including the new Saved Reading tests passed 150 top-level subtests / 159 tests; the full tracked suite passed 237 top-level subtests / 246 tests; `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. The build emitted only the existing plugin-timing advisory. No production data or local Wrangler D1 state was changed; Saved Reading tests used isolated in-memory D1-shaped fixtures. No deployment, merge or push is part of Saved Reading V1.

## NaTarot Reading Experience V1.3 (2026-09-19)
V1.3 is implemented in isolated branch `codex/tarot-reading-v1-3` from the clean V1.2 commit `4432911c0bf31531c9e641c6a14ceb2354c5f712`. At the V1.3 baseline, the Reader Engine prompt was `tarot-reading-v4.2` without a schema or reasoning-contract change. Its Vietnamese voice rules are customer-first and conclusion-first, speakable aloud, concrete before abstract, clear rather than artificially profound, restrained with therapy-speak, rhythmically varied, limited to at most one supported memorable line, and keep card-by-card explanation secondary under “Các lá bài nói gì?”. Follow-up remains `tarot-follow-up-v1`; Knowledge Base V5, provider configuration, cards, spreads, persistence, Saved Reading V1, auth/F-001 and historical compatibility remain frozen.

The ReadingPanel keeps the left Tarot stage and follow-up boundary unchanged while making the right reading surface more editorial: conversational section labels, quieter spread/deck metadata, a borderless takeaway, a grouped supporting-material region, quieter Tarot evidence disclosures, and an 820px content measure. Existing ordinal rendering remains `01`, `02`, `03` with no duplicated prefix. Visual QA covered 1440×900, 1280×720, 390×844 and 375×812; the browser captures showed no horizontal overflow. The original legacy v2 QA fixture remained unavailable through the current compatibility validator with HTTP 409, so no compatibility behavior was changed. No provider smoke call was made and no DeepSeek request was generated.

Validation: the focused reading/Room/Saved Reading/identity matrix passed 68 tests, the final presentation contract test passed 4/4, all Tarot tests passed 169 tests, the full tracked suite passed 267 tests, `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. The build emitted only the existing plugin-timing advisory. No deployment, merge or push is part of V1.3.

## NaTarot Reader Engine V4.2.1 (2026-09-19)
V4.2.1 is a prompt-only natural-voice refinement on the isolated V1.3 worktree. The main prompt version is `tarot-reading-v4.2.1`; the JSON schema, Knowledge Base V5, provider/model configuration, persistence, cards, spreads, follow-up prompt, Saved Reading behavior, F-001/auth boundary, and historical v2/v3/v4.1 compatibility remain unchanged. The new guidance normally confines card names to `card_evidence`, requires cross-section deduplication, removes somatic/therapy-like templates and arbitrary timing, favors simpler spoken Vietnamese, and keeps the primary answer situation-first rather than card-by-card.

Validation: the focused prompt/AI/reading/Saved Reading/identity matrix passed 179 tests across 170 top-level subtests; the full tracked suite passed 267 tests across 258 top-level subtests; `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. One real DeepSeek evaluation used the existing local provider configuration, Knowledge Base V5, the `spread-business-finding-your-magic` catalog spread, and the exact requested four-card context. It returned HTTP 200 from `deepseek-flash` with prompt version `tarot-reading-v4.2.1`, prompt/completion/total tokens `13525/2369/15894`, and approximately 14.3 seconds response time. The request count was exactly one; the payload was parsed successfully and was not persisted because the read-only D1 evaluation used an in-memory save adapter. Objective comparison found zero card-name mentions outside `card_evidence`, but residual semantic repetition and unsupported week-based timing remained; no further prompt tuning was made. No deployment, merge or push is part of V4.2.1.

## NaTarot Reader Engine V4.2.2 (2026-09-19)
V4.2.2 is a single bounded prompt-only naturalness pass on the isolated V1.3 worktree. The main prompt version is `tarot-reading-v4.2.2`; the JSON schema, Knowledge Base V5, provider/model/temperature/timeout configuration, persistence, cards, spreads, follow-up prompt, Saved Reading behavior, F-001/auth boundary, and historical v2/v3/v4.1 compatibility remain unchanged. The new contract explicitly prefers simple spoken Vietnamese over constructed depth, requires conclusion-first 2–3 paragraph answers, makes `deeper_reading` null by default, requires semantically new insights, prohibits invented timeframes unless supplied by the question/context, and favors ordinary Vietnamese titles. No UI or Reading Experience code changed.

Deterministic validation passed: focused prompt/AI tests `35/35`, all Tarot tests `169/169`, full tracked tests `267/267`, `npx tsc --noEmit`, `npm run build`, and `git diff --check`. One intentional non-persisting DeepSeek evaluation used the existing local provider configuration, Knowledge Base V5, the `spread-business-finding-your-magic` catalog spread, and the same four cards/positions as V4.2/V4.2.1. It returned HTTP 200 from `deepseek-flash` with prompt version `tarot-reading-v4.2.2`, prompt/completion/total tokens `14007/1759/15766` (including `1920` cached prompt tokens), and approximately `10.5s`. The provider request count was exactly one; the parser succeeded; the save adapter was a no-op over read-only local D1, so no reading or database state was persisted.

The parsed payload had three direct-answer paragraphs, two personal insights, two reflection prompts, two next steps, four card-evidence items, `deeper_reading: null`, and three follow-ups. IDs `cups-five`, `wands-eight`, `major-the-empress`, and `swords-queen` and positions `gift`, `magic`, `yearning`, and `beacon` were preserved. Objective review found no prohibited redraw invitation, no invented calendar timeframe, no third-party claim (the test was career/self-direction), and concise position-specific card evidence. Remaining observations are documented without another tuning call: three card names appeared outside `card_evidence` in `direct_answer`; the second personal insight substantially overlaps the direct-answer “cho đi nhiều hơn / tự rút cạn” point; and residual constructed/abstract wording includes `đà đó đang bị hút về phía điều chưa thành`, `công việc ... được nuôi dưỡng`, and `phần năng lượng đang chảy nhanh kia`. The V4.2.2 result is structurally valid and clearer, but it should receive human review before broader topic evaluation. No deployment, merge or push is part of V4.2.2.

## NaTarot Provider Reliability V1 (2026-09-20)
Provider Reliability V1 adds internal, metadata-only failure stages for provider response failures: HTTP JSON, provider envelope, missing content, content JSON, reading schema, card evidence count, card identity and position key. The route keeps the existing safe 503 response and never logs provider/customer content, prompts, authorization data, cookies, session tokens or generated prose. Diagnostics include only allowlisted stage, numeric counts/statuses, schema path/code, provider/model, prompt version, attempt number and retry decision.

The reading service performs one fresh provider generation only when the first generation raises `TarotAIError.code === "invalid_response"`. Both attempts reuse the same trusted session/card/context input; persistence occurs once only after a valid payload, and two invalid attempts persist nothing. Configuration, timeout, upstream, rate-limit and database/service failures are not retried by this application-level boundary. Existing transport behavior remains unchanged. The prompt remains `tarot-reading-v4.2.2`, Knowledge Base V5, schema semantics, Saved Reading, follow-up, auth/F-001, UI, cards, spreads and migrations are unchanged.

Validation on the isolated base `bce0f4c3a95319a97e1cf0a1dd35224d27087b56`: focused provider/parser/route/service/API tests passed `77/77`; all Tarot plus F-001/request-identity tests passed `180/180` tests across `171` top-level subtests; the full tracked suite passed `272/272` tests across `263` top-level subtests; `npx tsc --noEmit`, production `npm run build`, and `git diff --check` passed. No DeepSeek request, deployment, merge or push was performed.

## NaTarot Level 4 canonical baseline integration (2026-09-20)
The canonical Level 4 product baseline was verified after a branch-scoped fetch: `origin/codex/tooling-and-version-history` remained exactly `4db459a016f6335fc94a044a76318663c1b40af5` (`feat: restore approved member authentication`), with no remote advancement during this audit. The clean integration worktree started at that product commit on `codex/natarot-level4-baseline`; no product implementation, deployment, merge, rebase, reset, or push was performed.

Governance was imported only from `c5c1851aedb0dbd474e66a302356331958e0bfdf` for the Level 4 documents and retained historical bootstrap plan. The older `docs/PROJECT_STATE.md` from that governance branch was not replayed: this entry is added to the newer canonical state file so the later product history through Provider Reliability V1 remains intact. The governance documents were re-audited against the actual `4db459a` source, including local member sessions, Google OAuth, verification/reset email boundaries, the two `0004` migration files, F-001 identity-boundary coverage, metadata-only provider diagnostics, one invalid-response retry, and the V1.3/v4.2.2 reading surfaces.

Security status remains observational. The canonical source rejects spoofed `oai-authenticated-user-*` headers and has the tracked F-001 boundary test; no `app/chatgpt-auth.ts` platform-auth implementation exists. The bearer-style guest cookie and missing-`Origin` behavior remain documented as future security work and were not changed. Dirty F-001/auth worktree changes and dirty Home/CSS/Project State work were preserved outside this integration.

Canonical-source validation for this integration: `npx tsx --test tests/*.test.ts` passed 339/339 tests; `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. `npm run lint` remains a baseline failure with 89 errors and 124 warnings from existing application/generated/legacy rules; no application files were changed to address it. The integration is governance-only and is not a production deployment or production-readiness approval.

## NaTarot L4 Auto Topic + Auto Spread (2026-09-20)
Implemented on `codex/natarot-auto-topic-spread-l4`, based on the verified L1C SpreadBoard branch `codex/natarot-level4-spreadboard-l1c` at `a53dc44d5c088f2e9685e31f3e4a9dc51a09ebea`. The local deterministic bilingual recommender detects one of the existing `work`, `relationships`, `changes`, `creativity`, `magic`, or `idk` topics, classifies intent, and resolves every result through `currentSpreadCatalog` with card-count validation and a safe everyday fallback. Create now shows a compact Auto recommendation and stores the exact `categoryId`, `spreadTemplateId`, detected topic and `spreadMode: "auto"` in the existing session draft. Room hydrates valid draft IDs before recomputing, defaults legacy state to Auto, marks picker selections as `manual`, and exposes a guarded return-to-Auto action. Manual selections remain authoritative until the user chooses Auto again.

The existing `/api/tarot/draw` payload, repository validation, draw plan, SpreadBoard, normalized geometry, cards, AI/Knowledge Base, auth/security, persistence schema, migrations and deployment paths were not changed. No database migration, provider call, second spread catalog, merge, deployment or L5 work was performed.

Validation on the isolated worktree: the focused Auto/Room/recommender/SpreadBoard/geometry/mobile suite passed `39/39`; `npx tsx --test tests/*.test.ts` passed `371/371`; `npx tsc --noEmit`, `npm run build`, and `git diff --check` passed. New recommender/UI test files pass targeted ESLint. The touched-file ESLint command still exits nonzero on the pre-existing L1C Room baseline (`25 errors, 8 warnings`), with no new L4 lint delta identified.

Local browser QA used the isolated preview at desktop plus explicit `390x844` and `375x812` viewports. English and Vietnamese Create questions displayed their catalog-backed Auto cards; the narrow Create surfaces reported no horizontal overflow, with the recommendation card measuring `337px` and `321.18px` respectively. Create navigated to `/room?ritual=1`; Room restored the question and exact Career Crossroads/Getting Unstuck recommendation, manual Celtic Cross selection changed the picker to `Return to Auto`, and the Auto action restored the recommendation. The 375px Room picker also had no internal horizontal overflow. Browser console warning/error logs were empty. The local preview showed the existing generic save/catalog status while its API fallback was unavailable; this did not block the local catalog-backed Auto/manual flow and no API/runtime code was changed.

## NaTarot Credits / VIP Implementation V1 (2026-09-21, source-only)
Credits/VIP V1 is implemented on `codex/natarot-credits-vip-v1`, based on the verified integration commit `f5452a35d7b4bbc9884e52d0890c4f5e9c39e9fe`. The architecture-reference branch was read as documentation only; it was not merged wholesale. The implementation adds forward-only migration `0006_credits_vip.sql` and matching Drizzle declarations for member credit accounts, append-only credit grants/ledger entries, reservations and lot allocations, package/version catalog, orders, time-bound VIP entitlements, fulfillment and a narrow commercial-event boundary. Ledger fingerprints, unique idempotency keys, account mutation guards, expiry-aware FIFO allocation, lease-expiry recovery, projection rebuilds, owner scoping and compensating refund/adjustment/expiration entries are covered by isolated SQLite/D1-shaped tests.

Member billing balance/history/entitlements, active package projection, pending-order creation and owner-scoped order lookup are exposed through no-store authenticated APIs. Payment confirmation is an internal boundary only; no provider, webhook/IPN, SePay, affiliate or admin/RBAC surface was added. The Profile received only a restrained Credits/VIP status card. Authenticated Tarot readings reserve one credit and consume it only after the owner-scoped reading is persisted; known provider/persistence failures release the hold, duplicate requests reconcile persisted readings, concurrent requests do not double-charge, and guest readings retain the existing free path. Tarot prompt/provider semantics, Knowledge Base, spread behavior, Share behavior and existing auth/cookie behavior remain unchanged.

Validation on the isolated worktree: the complete tracked suite passed `470/470`; `npx tsc --noEmit`, `npm run build`, targeted ESLint for the new Credits/VIP and Tarot seam files, and `git diff --check` passed. Migration tests cover fresh and `0000`–`0005` upgrade paths; repository tests cover grant idempotency, FIFO allocation, refunds/adjustments, expiration, lease recovery, projection rebuild and two-concurrent-reservation overspend prevention; package/order/VIP, authorization, route-error, IDOR and Profile UI contracts pass. Repository-wide lint remains a pre-existing baseline failure in legacy/generated sources; no new lint exception was added. No production or staging checkout/database was changed, and no deployment, merge or provider credential action was performed. Next milestone: product-approved pricing/payment integration and operational reconciliation can be designed separately; it is intentionally not part of this source-only V1.

## NaTarot Full Feature Staging V1 (2026-09-22)
The isolated release branch is `codex/natarot-full-feature-staging`, created from the newest safe local Integration V3 head `b1bf0f6` in the managed worktree `/Users/tranquangthanh/.codex/worktrees/natarot-full-feature-staging/test astra`. It adds only the minimum functional `/packages`, `/checkout` and `/affiliate` route surfaces and Account navigation links, preserving the existing NaTarot/Moonlight shell and all frozen Tarot, AI, Share, Credits/VIP, SePay, Affiliate, Audit and RBAC semantics. The implementation commit is `158379d`; the independent review correction `672de4a` rotates checkout idempotency keys on package change and preserves the selected package through auth return URLs.

The full tracked suite passes `527/527`; `npx tsc --noEmit`, production `npm run build`, `npm audit --omit=dev` (`0` vulnerabilities), targeted ESLint, diff checks and staged secret scans pass. A read-only code review found no Critical or High findings; its two Medium checkout findings were fixed and retested. The release was packaged without `.git`, docs/tests, `node_modules`, Wrangler state, env files, logs or database files, then deployed only to `https://staging.natarot.com`.

Staging deployment is PASS. Before each release replacement, the staging SQLite database was copied into `/var/lib/natarot-staging/backups/` with mode `600` and integrity checked; the prior app release remains under `/opt/natarot-staging.before-full-feature-fix-20260921T191335Z` for rollback. Only `natarot-staging.service` was stopped/started; the service is active as `natarot-staging:natarot-staging` on `127.0.0.1:8788`, the app directory is mode `700`, the DB is mode `600`, and the checked-in migration chain is applied through both historical `0007_*.sql` filenames. Post-deploy metadata checks report integrity `ok`, zero foreign-key violations, 78 cards, 312 meanings, 57 spread templates and 174 spread positions. A final staging-only restart/readiness probe passed: systemd returned active, `/api/health` reached `200` with `{"status":"ok"}` within the bounded poll, the post-restart DB remained integral, and production stayed active. An immediate proxy probe can catch the short Vinext listener warm-up before readiness; this is documented as operational timing, not a release failure. Production apex/www returned `200` and `natarot.service` stayed active; no production app, DB, env, Nginx host, payment, credential or restart action was performed.

Live staging checks passed HTTPS/HTTP redirect, health/no-store/noindex, auth login/me, owner account summary/history, billing reads, package discovery, server-priced checkout `201`, display-only return `200`, provider reconciliation `200`, guest catalog/draw/session, safe no-provider reading `503`, F-001 spoofed-header rejection, and invalid public-share probes. The authenticated browser pass verified `/packages`, `/checkout` and `/affiliate`; exact CSS widths 375px, 390px and 412px showed the expected headings and no horizontal overflow. The complete SePay payment → IPN → fulfillment UI loop, DeepSeek generation, Google OAuth, Resend email delivery, fresh provider-backed Share loop, live Affiliate mutation/reversal fixtures and live Admin role mutations remain explicit external gates because staging has SePay Sandbox variable names only and no DeepSeek/Google/Resend runtime keys. The feature-by-feature evidence matrix is `docs/reports/natarot-full-feature-staging-feature-matrix.md`.

## Remaining work
Continue reference fidelity review, physical-device motion tuning and provision real service accounts. Full cross-device smoothness and production integrations remain incomplete.

## Versioning
Baseline commit 64fca6d, tag baseline-before-skills-2026-09-16. GitHub origin: https://github.com/zerotqt001-blip/vintarot (private). Baseline is on main; tooling is on codex/tooling-and-version-history. Push completed changes and verify remote refs; report any failures.

## Hosting
VinTarot is deployed on Sites at `https://vintarot-natarot.quang01.chatgpt.site` and the new public mobile-focused Site is live at `https://vintarot-natarot-mobile.e55009717.chatgpt.site`. Both use the current card-back artwork, 3D cylinder shuffle, question prompts, and bilingual interface. Anyone with either URL can view the corresponding Site until the user requests a different audience. Do not expose stored credentials.

Custom-domain cutover on 2026-09-18: BKNS DNS now routes both apex A records for `natarot.com` to `42.96.5.114`, and `www.natarot.com` is a CNAME to `natarot.com.`. Existing Sites validation TXT records were left untouched, and the old Sites URLs remain available for rollback. Public DNS and authoritative BKNS checks resolve to the VPS.

VPS deployment on 2026-09-18: BKNS `Cloud VPS Lifetime 1` is serving the app on Ubuntu 24.04.4 with Node `v22.14.0`, 2 cores, 2 GB RAM, 30 GB disk, and public IPv4 `42.96.5.114`. The Node/Vinext service is enabled and active as `natarot.service` on `127.0.0.1:8787`; Nginx `1.24.0` proxies both hosts and redirects HTTP to HTTPS; Certbot manages a Let's Encrypt certificate covering `natarot.com` and `www.natarot.com` with expiry on 2026-12-17 and scheduled renewal. UFW is enabled with only TCP `26266`, `80`, and `443` allowed. The deployment keeps the prior `/opt/natarot.previous-20260918-01` and latest `/opt/natarot.previous-20260918-134735` trees for rollback and does not copy `.env.local` or provider credentials.

The VPS runtime uses the checked-in Node 22 `node:sqlite` adapter and stores its database at `/var/lib/natarot/natarot.sqlite` outside the repository. The idempotent `0000`–`0004` migrations completed successfully with 78 Tarot cards, 312 bilingual orientation meanings, the normalized reading payload column, and five recorded migrations. This database is a fresh seed; no D1 user rooms, records, or reading history were migrated. Cloudflare Sites/D1 remains available as the rollback path.

Pre-DNS host-override verification passed on 2026-09-18: requests to `42.96.5.114` returned homepage/catalog `200`, draw `201`, session `200`, guest room `200`, guest journal `200`, and the no-provider reading contract `503` with the safe unavailable message. After the confirmed BKNS cutover and Certbot setup, public HTTPS verification returned `200` for both hosts, `301` for both HTTP-to-HTTPS redirects, and passed the cookie-backed catalog → draw → session → room → journal flow. The Room loaded at `https://natarot.com/room?ritual=1` with no browser console errors. The guest `vintarot_guest` cookie follows the effective proxied protocol and is `Secure` on HTTPS. No AI provider key/model is configured on the VPS, so live AI readings remain intentionally unavailable until a provider is provisioned.

The HTTPS smoke test initially exposed an origin-check mismatch behind Nginx; `lib/server.ts` now compares the browser Origin with the forwarded public protocol and Host, with a regression test in `tests/server-origin.test.ts`. Focused tests pass 5/5, `npx tsc --noEmit`, production build, `git diff --check`, public TLS checks, and the guest API flow all pass after redeployment.

Final public verification for the personal-centric Tarot release: `https://natarot.com/` and `/room?ritual=1` both returned `200`; `/api/tarot/catalog?locale=en` returned `200`; the cookie-backed guest flow returned draw `201`, session `200`, room `200`, and journal `200`; `/api/tarot/reading` returned the expected `503` because no provider is configured. The public HTML references the new production Room chunk and the browser accessibility tree exposes the current NaTarot controls. The generated `dist/` artifact was built locally with `npm run build` and checked for secret-like content before transfer.

## Verification
`npx tsc --noEmit`, `npm run build`, the tracked test suite (41/41, including the cards-to-Guidebook navigation regression), `npx tsx --test tests/create-celestial.test.ts`, and `git diff --check` pass for the bilingual long-form Guidebook narrative without Meme sections, locale, shuffle, Guidebook, Room visual interaction, curved-label, homepage Celestial and three-screen cosmic Guidebook work plus the dynamic Create flow, its reference-aligned composition and Celestial Practice composition. `npm run lint` remains red on pre-existing generated skill/runtime files and legacy Next anchor rules; the new UI code adds no lint-specific exception. Browser checks additionally confirm Moonlight CDN image URLs and captions in Guidebook, the responsive constellation layout with localized group labels and clickable group dialogs, a complete face-up card without the extra border in Room, Vietnamese tarot guidance for Queen of Cups, and the full scrollable English/Vietnamese The Fool narrative with distinct upright/reversed sections. The production archive was validated and the first private Sites deployment succeeded. Browser checks covered English/Vietnamese switching, the NaTarot homepage at desktop/tablet/mobile sizes without horizontal overflow, cosmic hero and glass daily panel composition, curved Home navigation and Create Ritual labels against Moonlight, Create topic suggestions, the two-step `/create` reference flow at desktop in English and Vietnamese with heading focus on step changes, clickable topic labels and persistent suggestion icons, the continuous 3D-cylinder Room shuffle, two-tap stop/settle, fan draw, card flip, translated spread controls, Guidebook world selection, library search/filter/sort, uncropped card art, upright/reversed details, detail arrow/thumbnail navigation, history-preserving back links, keyboard and touch navigation, Moonlight artwork fallback behavior, symmetric spread-card tilt/lift, contextual prompts, bounded table pan controls, direct tabletop drag and card placement alignment, pinch/wheel gesture helpers, and the layered Celestial Luxury Room backdrop with perspective drift, glass panels, question hierarchy, deck instruction and tablet card spacing. This session additionally verified the English and Vietnamese navigation labels, the Cards link resolving to `/guidebook`, and the legacy `/decks` URL rendering the Guidebook without a deck switcher. Full fidelity, mobile visual capture and production integrations remain incomplete.

The original replacement Site remains live at version 5 from its prior published source. The newly requested separate public Site is live at version 4 from the exact published source commit `c238cc8a63eb294563916e15a778f8a65977a28c`; its public URL is `https://vintarot-natarot-mobile.e55009717.chatgpt.site`.

The 2026-09-18 DeepSeek verification configures the selected local provider as `deepseek` with `deepseek-flash`; the API key remains only in the ignored `.env.local` and was not committed or logged. DeepSeek's default thinking mode caused the structured Tarot request to exceed the previous 10-second transport window, so the DeepSeek transport now explicitly disables thinking for this JSON reading and allows up to 20 seconds while preserving the existing retry/error handling. A live provider smoke test returned a valid structured three-card reading; repository verification remains required before deployment.

Production AI activation and recovery (2026-09-18): after explicit user confirmation, the selected DeepSeek provider configuration was transferred over SSH into the VPS-only `/etc/natarot.env` file; the secret value was never printed, committed, or returned by the API. The first production request reached DeepSeek but exposed a strict-contract formatting mismatch because `direct_answer` arrived as one paragraph. Commit `4e6e3e6` adds the prompt instruction and regression coverage requiring 2–4 blank-line-separated paragraphs, then was rebuilt and deployed with the verified `dist/` artifact. The active `natarot.service` now serves the fixed release. A fresh public guest flow returned HTTP `200` from `/api/tarot/reading` with three card-evidence items, three direct-answer paragraphs, personal insights, reflection prompts and next steps; the production Room retry was also verified in the browser and rendered the complete reading panel.

DeepSeek schema follow-up (2026-09-18): a second exact-room retry exposed that the provider could still return an out-of-bounds `reflection_prompts` array even when the JSON was otherwise valid. Commit `7cd410d` adds explicit cardinality requirements for personal insights, reflection prompts, next steps, card evidence and follow-up suggestions to both the system prompt and JSON contract. The Tarot suite passes 137/137, TypeScript/build/diff checks pass, the release is deployed on the active VPS service, and the exact public room retry now returns HTTP `200` and renders the complete Vietnamese reading panel. The latest production log records the successful DeepSeek response with four cards and prompt version `tarot-reading-v3`.

## NaTarot L5–L8 Engine (2026-09-21)
L5, L6, L7 and L8 are complete on the isolated branch `codex/natarot-l5-l8-engine`, based on the verified L4 commit `f92b073e69e57462f5ea898d2fe8a521ac2c4fac`. Checkpoints are `8a1306f` (whole-spread semantics), `5723527` (validated provider delivery), `0e0090c` (contextual follow-up suggestions), `bfe2904` (server-authoritative supplementary clarification draws), and `c274ff7` (review hardening for concurrent idempotency, persisted-card validation, locale rendering and active-reading hydration). The branch is not merged and no deployment was performed.

L5 derives deterministic localized spread purpose, suitability, strategy, emphasis and bounded position relationships from the existing catalog and carries those semantics into the trusted AI context without changing spread geometry or IDs. L6 keeps final validated JSON as the only authoritative delivery, with bounded retry handling and no partial persistence. L7 deterministically normalizes up to three question-shaped, non-redraw suggestions from the saved reading before persistence. L8 stores optional `supplementaryDraws` inside the existing nullable `readings.reading_payload` JSON: ownership/session validation, server-side unused-card and orientation selection, maximum three draws, request-id idempotency, in-process coalescing for concurrent duplicate requests, session-scoped compare-and-set persistence, focused clarification context and bilingual minimal UI are all implemented. Persisted history also rejects duplicate, foreign-deck and original-reading card IDs. No migration, auth, Knowledge Base V5, business-feature or deployment change was introduced.

Verification on the isolated worktree: `npx tsx --test tests/*.test.ts` passed `402/402`; `npx tsc --noEmit`, `npm run build`, targeted ESLint for every changed/new file outside the legacy `app/room/room.tsx`, and `git diff --check` passed. The touched `app/room/room.tsx` remains within the existing lint baseline (`25 errors, 6 warnings`); repository-wide `npm run lint` remains the pre-existing `89 errors, 124 warnings` baseline. Local browser QA completed Create → Auto recommendation → Room → shuffle → three-card draw → reading fallback on desktop, then checked the reading surface at 390px and 375px with no overflow observed. Provider-backed reading and clarification CTA execution were not claimed because no local AI credentials were configured; the clarification service, route, provider adapters, ownership, idempotency, retry, UI and follow-up regressions are covered by the automated suite. The catalog contains a 10-card Celtic Cross and no 12-card template; no provider-backed large-spread browser run was performed.

External research loop: current NaTarot provider, repository, session, draw, prompt and persistence boundaries were audited first. Official DeepSeek documentation was checked for Chat Completions JSON Output, SSE `[DONE]` behavior, keep-alive comments, concurrency/rate-limit handling and safe error classes; official Next.js Route Handler/Web Streams documentation and MDN Fetch/ReadableStream/AbortController references were checked for the actual runtime primitives. `J-York/TarotWhisper` and `anois/tarot` were reviewed for spread validation, normalized position contracts, whole-board context, bounded follow-up history, supplementary-card flow, SSE parsing and license/asset boundaries. `eventsource-parser` and Vercel AI SDK structured streaming were reviewed as possible dependencies. The smallest compatible choice remains the existing NaTarot implementation plus Web Platform primitives and Zod: no new dependency, no copied external code, no provider token streaming for authoritative readings, final-only schema validation/persistence, and the existing one-invalid-response retry with in-process duplicate coalescing plus database CAS. TarotWhisper and anois/tarot remain architecture references only; their source, prompts, card assets and share/QR systems were not copied. License review found MIT source licenses for the named code references, while their card-art/data caveats remain separate and do not alter NaTarot assets. Future research may revisit provider-backed progress streaming only if a concrete UX requirement justifies the runtime and cost surface.

Status: L5 COMPLETE / VERIFIED / PUSHED; L6 COMPLETE / VERIFIED / PUSHED; L7 COMPLETE / VERIFIED / PUSHED; L8 COMPLETE / VERIFIED / PUSHED; NOT MERGED; NOT DEPLOYED. Remote verification is recorded after the final branch push.

## NaTarot Master Integration V1 (2026-09-21)
The first authoritative integration branch is isolated as `codex/natarot-integration-v1` from exact L5–L8 engine base `046c166ffa653fdd985c81fcbde8d62b35634f15` in a separate worktree. The S6 share-persistence commit `9d7de6e971e1e610ed5c3d1343d77c4452dc8ee` was integrated once through merge commit `b9ce36e`; the Room conflict was resolved semantically so L5 whole-spread reading, L7 follow-up, L8 clarification, SpreadBoard behavior and S6 share creation/public/revoke controls coexist. Raw share tokens remain transient; SQLite/D1 stores only SHA-256 token hashes, derives ownership through the reading/session join, exposes only the public allowlist, and keeps analytics event IDs idempotent.

Only the verified verification-email implementation/tests from `ae7b9cfa203abc2a00b327d0acc6a0e3251b2843` were ported in `9fcd0b3`; the historical state document was not imported. The audited security changes from `57db368`, `40a1844` and `a8893db` were applied selectively in `eb13ba2`: no-store response boundaries, `/api/health`, the compatible React/RSC `19.2.8` and Vite `8.0.16` updates, and the preserved S6 QR dependencies. No divergent security/auth branch tree was merged.

Migration verification covers the ordered `0000`–`0005` set, including both canonical `0004_member_auth.sql` and `0004_reading_payload.sql`. Fresh and existing pre-share SQLite databases apply deterministically and idempotently; legacy member/session/reading rows survive the upgrade, `0005` is applied once, foreign keys are clean, and the token-hash, active-reading, status/privacy and cascade constraints are present. The upgrade regression is committed as `734ce26`.

Validation for the integrated code head `734ce26`: S6/engine focused suite `148/148`; email/auth focused suite `50/50`; final security/auth matrix `49/49`; migration/share focused suite `21/21`; complete tracked suite `441/441`; `npx tsc --noEmit`; production `npm run build`; `npm audit --omit=dev` with `0 vulnerabilities`; and `git diff --check` all pass. Targeted ESLint passes for the newly integrated email/security/migration files. The broader changed-source lint still reports the inherited engine/S6 baseline (`26 errors`, `7 warnings`), concentrated outside those selective files. L6 remains final validated JSON with bounded retry/persistence behavior; real token streaming is deliberately not implemented.

Operational boundary for this integration: `MERGED TO MAIN: NO`; `DEPLOYED: NO`; `PRODUCTION DB: UNCHANGED`; no production migration, VPS restart, provider credential, payment/Credits, Affiliate, or production secret action was performed. The branch was pushed normally as `codex/natarot-integration-v1`, and local/remote equality was verified at state commit `aaf6794d977fcb021f2cc6a33b37ee3109b34094`; no merge or deployment is part of this milestone.

## NaTarot Staging Environment V1 (2026-09-21)
The approved staging design and implementation are isolated on `codex/natarot-staging-v1`, based on exact source commit `f5452a35d7b4bbc9884e52d0890c4f5e9c39e9fe`. Repository artifacts define a separate `natarot-staging.service`, `/opt/natarot-staging`, `127.0.0.1:8788`, `/var/lib/natarot-staging/natarot.sqlite`, `/etc/natarot-staging.env`, and an independent `staging.natarot.com` Nginx/TLS host. The staging environment has no provider, SePay, payment, Credits, VIP, Affiliate, Google, Resend, or production-secret configuration; runtime data must be synthetic.

The staging deployment contract test, systemd unit, bootstrap and final Nginx hosts, and operator runbook are implemented. BKNS DNS is `PASS`: exactly one new A record `staging.natarot.com -> 42.96.5.114` with TTL 300 was added and all three authoritative BKNS nameservers plus public resolvers return the same address; existing production records were not changed.

Live staging deployment is `PASS` on BKNS `Cloud VPS Lifetime 1`: the isolated systemd service is enabled/active as `natarot-staging.service` under `natarot-staging:natarot-staging`, serves only `127.0.0.1:8788`, and uses `/var/lib/natarot-staging/natarot.sqlite` with `/etc/natarot-staging.env` containing only `NODE_ENV=production`. The new database applied migrations `0000`–`0005`, contains the seeded 78-card catalog and synthetic smoke rows only, and is owned `natarot-staging:natarot-staging` with mode `600`. Nginx serves the separate staging host with `X-Robots-Tag: noindex, nofollow`; HTTP redirects to HTTPS; the staging-only Let's Encrypt certificate covers `staging.natarot.com` through 2026-12-20.

Live smoke checks passed: HTTPS health/home/catalog, guest draw `201`, owner session `200`, no-provider reading `503`, F-001 spoofed OAI headers rejected at the proxy boundary, attacker Origin rejected, synthetic share create/public/image/event/duplicate/foreign-revoke/owner-revoke loop, and staging restart persistence. Production `natarot.service` remained active and public apex/www HTTPS returned `200` after the staging work. No production app tree, database, systemd unit, Nginx production host, provider configuration, or production secret was modified. SePay/Credits/VIP/Affiliate work remains intentionally not implemented.

## NaTarot Staging Integration V2 (2026-09-21, HUMAN_GATE)
The isolated `codex/natarot-staging-integration-v2` branch starts from Master Integration V1 `f5452a35d7b4bbc9884e52d0890c4f5e9c39e9fe`, integrates the verified Credits/VIP V1 commit `121e184a617f43df049407ff5981e17ba6613616`, and includes the previously verified staging infrastructure contracts. Local validation passed the focused integration matrix `291/291`, full tracked suite `474/474`, `npx tsc --noEmit`, production build, staging contract tests, targeted lint for changed/new files excluding the inherited `app/pages.tsx` baseline, secret-like tracked-value scan, and `git diff --check`.

The mission stopped before any VPS storage audit, cleanup, staging-only database backup, migration 0006, staging deployment/restart, live API regression, browser QA, or UI reality audit because the required read-only SSH probe to `42.96.5.114:26266` failed with `Permission denied (publickey,password)` for the local default account. No local SSH config or agent identity was available; no credential was requested or printed. Production was not accessed or modified. Remote artifacts remain unknown/untouched, and the next step requires an authorized SSH account/key or approved console path.

## NaTarot Master Integration V3 (2026-09-22)
The controlled V3 branch is `codex/natarot-integration-v3`, based on Backend Completion V1 `8409c8d`. Only the verified SePay implementation/fix commits `d6c204a`, `4443a04`, `3924326`, `44a9eb6` and `594e569` were ported from the divergent SePay branch. Historical branch-only documentation and merge snapshots were excluded. Conflicts in `db/schema.ts`, `lib/runtime.ts` and migration tests were resolved in favor of Backend Completion's newer Account/Affiliate/Audit/RBAC semantics while preserving the SePay payment-attempt/event and verified-fulfillment boundary.

Both historical `0007_backend_completion.sql` and `0007_sepay_commercial.sql` remain unchanged and are applied by full filename through `scripts/node-migrate.mjs`. Fresh and upgrade migration fixtures pass with clean foreign keys, duplicate-run protection, 78 cards, 312 meanings, Credits/VIP, Orders, Affiliate/Audit and SePay tables. The integrated tracked suite passed `524/524`; TypeScript, production build, `npm audit --omit=dev` (`0` production vulnerabilities), `git diff --check` and targeted ESLint for changed TypeScript/TSX files pass. Repository-wide lint remains an inherited baseline failure (`89` errors, `125` warnings) concentrated in legacy/generated files and `app/vintarot.tsx`.

Staging-only deployment is complete at `https://staging.natarot.com`. A timestamped staging SQLite backup was verified before the missing Backend Completion migration was applied. The staging service was the only service stopped/restarted; the prior release remains available for rollback. Post-migration integrity is `ok`, and staging retains one verified/fulfilled Sandbox payment, one 10-credit grant and one active VIP entitlement. Synthetic staging API checks passed health, member login/session, account summary, package discovery, checkout creation, idempotent replay and reconciliation. Production apex/www services, database and credentials were read-only and unchanged.

Desktop browser QA against staging verified the NaTarot/Moonlight shell and no detected overflow on Home, Guidebook, Daily Spread, Practice, Auth, Create, Admin and authenticated Profile. `/packages` is intentionally API-only and returns `404`; Profile still presents payment as needing setup, which is recorded as a P2 integration-boundary finding rather than a UI redesign. Live DeepSeek, full Share event/revoke/image/QR, Affiliate mutation/reversal, complete role matrix and fresh device-size mobile screenshots remain HUMAN_GATE items. No production payment, real-money transaction, production secret, destructive database action or production restart was performed. Evidence is in `docs/reports/natarot-integration-v3-staging-audit.md` and `docs/reports/natarot-integration-v3-browser-qa.md`; freeze and UI ownership are in `docs/FEATURE_FREEZE_V3.md` and `docs/design/NATAROT_FINAL_UI_HANDOFF.md`.

## NaTarot Backend Completion V1 (2026-09-22)
Backend Completion V1 is implemented on the isolated branch `codex/natarot-backend-completion-v1`, based on the verified product head `fd7d8fe14c86a282d9319d79fef73ee654ae5032` from Staging Integration V2. The branch adds forward-only migration `0007_backend_completion.sql`, versioned Affiliate V1 attribution/conversion/commission policy and ledger, application-level PII encryption/redaction primitives, append-only audit events, server-side RBAC/admin services, owner-scoped account/history read models and only the minimum functional `/account` and `/admin` surfaces. Existing Auth/F-001, Google/guest identity, Credits/VIP, Tarot/KB, L1/L4/L5/L7/L8, Share S1–S6, backup/DR and runtime boundaries remain protected. SePay, payout, provider payment verification, production KMS provisioning, physical identity/product database separation and final UI redesign are intentionally out of scope.

## NaTarot Full Feature Production V1 (2026-09-22)

The authorized Full Feature V1 source head `a0a79fa64aa398a566b40bef96be3c590bf5292d` from `codex/natarot-full-feature-staging` is deployed live at `https://natarot.com`. The candidate archive was built without environment files, databases, logs, dependencies, keys, certificates or credential-like filenames; its SHA-256 is `fdd8914b32c5fea8eb3e33a2682de24a6292cece7ff0116e6b13bd65a5d3d05b`. The active non-secret deployment revision is recorded by SHA-256 `b17b088acf1c6366e4210b655fea2a3eaecda21d54f4e035e8fe13ac30c58b60`, and the prior release remains at `/opt/natarot.rollback-full-feature-v1-20260922T070221Z`.

The mandatory production backup `natarot-production-20260922-064648` was checksum-verified and restore-tested. Its archive SHA-256 is `7d7434a72fbc07a77536511b1d933b309af759efcf334e90de9c30afa767d826`; live and extracted databases both reported SQLite integrity `ok` and zero foreign-key violations. Migration dry-run and live migration applied `0005_natarot_share_persistence.sql`, `0006_credits_vip.sql`, `0007_backend_completion.sql`, and `0007_sepay_commercial.sql`, producing ten clean migration rows without destructive operations.

Runtime verification is complete: the full tracked suite passed `527` tests, TypeScript/build/audit/diff checks passed, and only the inherited repository lint baseline remains (`89` errors, `125` warnings). Public E2E passed catalog/automatic topic/manual draw/Celtic Cross/guest session-journal-room persistence, a real DeepSeek reading with prompt `tarot-reading-v4.2.2`, and the full Share/public SVG/QR/analytics/idempotency/revoke loop. The same draw and live DeepSeek path passed after a service restart. Responsive live-site smoke at 375px, 390px and 412px had no measured horizontal overflow; the temporary viewport override was reset.

Security verification passed after adding HSTS to the production Nginx HTTPS host; `nginx -t` and reload passed, with the recoverable pre-change configuration at `/var/backups/natarot/configuration/natarot-before-hsts-20260922T072904Z`. Sensitive-path exposure, origin rejection, share privacy, runtime permissions, log leakage, database integrity and restart persistence all passed. This remote Nginx hardening is operational state, not a source-file change.

The deployed code covers Auth/F-001, Tarot/SpreadBoard/AI, Share/QR, Account/History, Credits/VIP/Packages/Orders, Google, Email, SePay, Affiliate and Admin/RBAC. Account/History, Credits/VIP, Google, Email, SePay, Affiliate and authenticated Admin/RBAC remain `DEPLOYED + EXTERNAL_GATE` where completion requires an owner/provider credential, approved production catalog, or safe authenticated fixture. Resend returned HTTP 401; the exact owner action is to replace the existing production Resend key with a valid key. Production packages remain empty by safety design, and staging SePay sandbox configuration was not copied. No real-money activation, secret rotation, UI redesign, or unrelated milestone began.

The final report is `docs/reports/natarot-full-feature-production-v1-final-report.md`. The branch remains a `codex/` branch and is intended to be pushed after the documentation commit; it must not be merged to `main` as part of this release.

External research is recorded in `docs/research/NATAROT_BACKEND_COMPLETION_RESEARCH.md`, with durable security/account/operations docs and the final report at `docs/reports/natarot-backend-completion-v1-final-report.md`. Better Auth, OpenFGA, node-casbin, Medusa affiliate/payment patterns, pametan audit-log, Node crypto and the SePay SDK were evaluated against the current NaTarot boundary; only compatible patterns and built-in WebCrypto were adopted. No external repository was wholesale merged or copied.

Validation on the isolated worktree: the complete tracked suite passed `502/502`; fresh and upgrade migration tests, focused Affiliate/RBAC/security/audit/account/UI suites and the adversarial security review pass; `npx tsc --noEmit`, `npm run build`, `git diff --check` and the changed-diff/tracked-HEAD secret scans pass. New/changed TypeScript and TSX files pass targeted ESLint when the legacy `app/vintarot.tsx` shell is excluded; the full changed-source lint retains 14 inherited Next internal-anchor errors in that shell and no new lint exception was added. No production/staging checkout, database, migration, service, credential or secret was changed. No merge, deployment, restart, payout or real-money action was performed.

The first normal push completed and local/remote equality was verified at `c53605d0c87b07a5db871656adae05ce0d03cb86` on `origin/codex/natarot-backend-completion-v1`; a source-only metadata commit records that status. The next controlled step is Integration V3: apply migration 0007 after 0006 locally, preserve the verified SePay-to-fulfillment event boundary, repeat the full suite and security review, then separately obtain production KMS/database-role/backup/retention/payment-contract gates. This branch is safe for review/integration and must not be merged or deployed automatically.
