# NaTarot Auth Recovery on 7012 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the previously working member Login/Register/password-reset/Google-auth surface on top of the production Room cleanup from `7012a923`, without restoring the old Room UI or weakening auth security.

**Architecture:** Use `7012a923` as the implementation base and take only the member-auth implementation and its direct server/navigation dependencies from `9885d464` (`codex/auth-current-ui`). Preserve the 7012 Room component and its mobile/desktop interaction behavior; apply only the auth-aware user lookup, profile destination, and draw-resume integration required by the member-auth contract. Add thin route aliases that render the existing `AuthScreen` so the required public `/login`, `/register`, `/forgot-password`, and `/reset-password` entrypoints return `200` without duplicating auth logic.

**Tech Stack:** React 19, Vinext/Vite, TypeScript, Node `node:sqlite`, Drizzle migrations, `node:test`, systemd/Nginx production deployment.

**Spec:** User-provided production regression request pasted at `/Users/tranquangthanh/.codex/attachments/6b1ca456-1450-4909-9eaa-30f04915cf7e/Văn bản đã dán.txt`.

## Global Constraints

- Base the release on `7012a923bcbaa5b5e44bcc11f268a60db37afbff`; do not deploy the unrelated current checkout or `auth-current-ui` wholesale.
- Preserve the 7012 Room cleanup: no mobile Welcome panel, no `Sẵn sàng xáo bài`, no `.room-bottom`, deck/card/BookOpen interaction behavior unchanged.
- Do not change production secrets, OAuth credentials, Nginx/domain/SSL, database contents outside the required idempotent auth migration, or trusted-identity security behavior.
- Do not log credentials, OAuth tokens, cookies, or provider secrets.
- Use the bounded readiness poll: 30-second deadline, 1-second polling, success only on local HTTP 2xx, rollback on timeout or unhealthy service.

### Task 1: Establish the failing auth contract

**Files:**
- Create: `tests/auth-recovery-contract.test.ts`
- Reference: `app/vintarot.tsx`, `app/room/room.tsx`

- [ ] **Step 1: Write the failing test**

  Assert that the four public auth page entrypoints exist, the canonical `/auth` page exists, the auth API route tree exists, and the logged-out shell uses an auth destination rather than `/profile` for its profile/login entrypoint. Also assert that the 7012 Room source still has the cleanup markers absent/preserved by the release contract.

- [ ] **Step 2: Run the focused test and verify RED**

  Run `npx tsx --test tests/auth-recovery-contract.test.ts` from the isolated worktree. It must fail because 7012 has no `app/auth` or `app/api/auth` implementation and its logged-out profile link still points to `/profile`.

### Task 2: Integrate the member-auth implementation only

**Files:**
- Create from the reviewed 9885 implementation: `app/api/auth/**`, `app/auth/**`, `components/auth/auth-shell.tsx`, `lib/auth-email.ts`, `lib/auth-handlers.ts`, `lib/auth-rate-limit.ts`, `lib/google-oauth.ts`, `lib/member-auth.ts`, `lib/member-page.ts`, `drizzle/0004_member_auth.sql`.
- Modify with reviewed auth-only hunks: `app/page.tsx`, `app/[section]/page.tsx`, `app/room/page.tsx`, `app/vintarot.tsx`, `app/pages.tsx`, `app/room/room.tsx`, `components/language.tsx`, `lib/i18n.ts`, `lib/client.ts`, `lib/request-identity.ts`, `lib/runtime.ts`, `lib/server.ts`, `lib/sqlite-d1.ts`, `lib/tarot-guest.ts`, `lib/tarot-interpretation.ts`, `build/sites-vite-plugin.ts`, `vite.config.ts`.
- Do not import unrelated 9885 Room layout/guide/shuffle changes; keep the 7012 versions of Room cleanup code and CSS except for the smallest auth destination/user-type changes.

- [ ] **Step 1: Integrate pure auth files and migration**

  Copy the exact reviewed files from `9885d464` into the 7012 worktree, then inspect every import against the 7012 tree. Preserve F-001 trusted-identity behavior from 7012 and include only the auth-compatible request identity/server changes required by the reviewed auth tests.

- [ ] **Step 2: Apply auth-aware shell/page wiring**

  Port the `getPageMember`/member-shell-user wiring and logged-out profile destination from 9885 while retaining 7012 Room state defaults, mobile guide removal, shuffle handler, pointer guards, and BookOpen behavior.

- [ ] **Step 3: Add non-duplicating public route aliases**

  Add `/login`, `/register`, `/forgot-password`, and `/reset-password` pages that call the existing `AuthScreen` with modes `login`, `register`, `forgot`, and `reset`; forward `return_to` and `token` through the existing safe-path/token handling. Do not create a second auth handler or bypass any validation/security check.

- [ ] **Step 4: Run focused auth tests and verify GREEN**

  Run the new contract test plus the reviewed auth unit/UI tests. Confirm the tests pass without changing the 7012 Room tests.

### Task 3: Verify build, route behavior, and Room regression

**Files:**
- Test only: `tests/auth-recovery-contract.test.ts` and the existing auth/Room test files.

- [ ] **Step 1: Run auth and Room test suites**

  Run the focused auth tests, `npx tsx --test tests/room*.test.ts`, and the relevant server/origin/request-identity tests. Do not weaken assertions.

- [ ] **Step 2: Run typecheck and production build**

  Run `npx tsc --noEmit`, `npm run build`, and `git diff --check`; inspect generated route chunks and secret-scan the release archive.

- [ ] **Step 3: Run local public-route smoke tests**

  Start the built Vinext server on an unused local port and assert `200` for `/auth`, `/login`, `/register`, `/forgot-password`, `/reset-password`, and the expected Google entry route status without following external OAuth. Assert that `/room` still has no old CTA/bottom-bar markers and that its new bundle retains the 7012 Room behavior.

- [ ] **Step 4: Run browser checks**

  In logged-out state, locate the public profile/Login entry, verify its actual href, click it, and confirm the auth screen renders. Test mobile and desktop. Verify reset renders only with its token contract and Google start either redirects to Google when configured or returns the existing safe unavailable response without exposing secrets.

### Task 4: Deploy atomically and verify public acceptance

**Files:**
- Remote release directory: `/opt/natarot`
- Remote rollback directory: a new `/opt/natarot.previous-<release>` retaining the current production 7012 tree.

- [ ] **Step 1: Preflight production and record rollback**

  Confirm the active bundle is the deployed 7012 artifact, `natarot.service` is healthy, the current public auth regression is reproducible, and the existing rollback point is retained. Do not modify `/etc/natarot.env`.

- [ ] **Step 2: Stage and checksum the release**

  Archive only the verified source/build output, exclude secrets, databases, logs, `node_modules`, and `.codex`, upload to a uniquely named remote staging path, and compare SHA256 checksums before activation.

- [ ] **Step 3: Swap, restart only `natarot.service`, and poll readiness**

  Atomically retain the current `/opt/natarot`, activate the candidate, run `systemctl restart natarot.service`, poll `http://127.0.0.1:8787/room` once per second for up to 30 seconds, and restore the rollback directory if readiness or service health fails.

- [ ] **Step 4: Verify public production behavior**

  Check the public `/login`, `/register`, `/forgot-password`, `/reset-password`, `/auth`, Google entry/completion contract, logged-out Login navigation, and the exact Room acceptance at 390×844 and 1440×900. Verify no console errors, no horizontal overflow, and no old Room UI markers.

- [ ] **Step 5: Report evidence**

  Report root cause, auth source commit/branch, exact integrated files, previous/deployed revisions, readiness timestamps/attempts, route statuses, Room regression results, test/typecheck/build results, rollback path, and any intentionally unresolved issue.
